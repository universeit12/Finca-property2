import 'package:flutter/material.dart';

class AllPropertyDetails extends StatelessWidget {
  const AllPropertyDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("All Property Details"),),
    );
  }
}
